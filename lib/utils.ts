// Magic UI / shadcn CLI components import `cn` from "@/lib/utils".
// Our project's actual implementation lives in lib/utils/cn.ts. This
// re-export keeps both call paths valid without duplicating code.
export { cn } from './utils/cn'
